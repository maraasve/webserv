Incoming data:
- Client -> request
- CGI -> output

Ougoing data:
- Client -> response
- CGI -> input


//if (event_fd & EPOLLIN)
	//readIncomingData
	// if clientFD
		//ParseRequest(incomingData)
		//are we accepting inside the client (epoll.add(cgi_fd, EPOLLIN??))
	// else CGI FD
		//